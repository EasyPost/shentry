name: 'CI'

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: set up python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
      - name: install dependencies
        run: make install
      - name: lint with flake8
        run: make lint
  run-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pythonversion: ['3.7', '3.8', '3.9', '3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@v6
      - name: set up python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.pythonversion }}
      - name: install dependencies
        run: make install
      - name: test with pytest
        run: make coverage
      - name: Coverage
        if: github.ref == 'refs/heads/master'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
